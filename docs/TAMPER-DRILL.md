# Ledger tamper drill — results

<!-- GENERATED by `npm run drill:tamper -- --report docs/TAMPER-DRILL.md`. Do not edit by hand. -->

A scripted adversary with write access to the sqlite file mutates a **copy** of `dataset/reference-ledger.db` one class at a time, with targets drawn from a seeded generator, and the verifier must catch every one. Seeds `1, 2, 3, 4, 5, 6, 7, 8` · 34 in-model classes · 272 cases.

**Negative control** — an untampered copy of the same ledger: **VERIFIES** (5 receipts). A drill whose verifier rejects everything proves nothing, so this line comes first.

**Result: 272/272 in-model tampers caught (100%) · 0 escaped.**

## In-model — every one of these must FAIL verification

| class | the lie it tells | cases | caught | located by |
|---|---|---:|---:|---|
| `append-forged-chain` | append a receipt that never happened, with plausible-looking chain columns | 8 | 8/8 | `row-altered` |
| `append-null-chain` | one plain INSERT with the chain columns left empty — the CHEAPEST forgery, and it never touches chain_state | 8 | 8/8 | `uncommitted-row` |
| `delete-interior-row` | make one inconvenient receipt disappear from the middle of the history | 8 | 8/8 | `chain-break` |
| `drop-a-session-scope` | delete a session's commitment so its ledger has nothing to check against | 8 | 8/8 | `missing-scope` |
| `duplicate-sequence-number` | collide two receipts on one sequence number so the order is ambiguous | 8 | 8/8 | `malformed-row` |
| `edit-add-a-finding` | frame a clean receipt by adding a finding to it | 8 | 8/8 | `row-altered` |
| `edit-agent` | reassign the blame to a different agent | 8 | 8/8 | `row-altered` |
| `edit-clear-a-finding` | flip a flagged receipt to balanced and erase its finding — the forgery the product exists to stop | 8 | 8/8 | `row-altered` |
| `edit-content-address` | repoint a row at a different receipt body | 8 | 8/8 | `row-altered` |
| `edit-delta` | shrink the discrepancy without clearing it | 8 | 8/8 | `row-altered` |
| `edit-merchant` | rewrite who the money went to | 8 | 8/8 | `row-altered` |
| `edit-receipt-id` | rename a receipt so a citation of it dangles | 8 | 8/8 | `row-altered` |
| `edit-timestamp` | move a receipt into a different reporting period | 8 | 8/8 | `row-altered` |
| `empty-a-chain-column` | set a chain column to '' — not NULL, so it dodges an IS NULL filter | 8 | 8/8 | `chain-break`, `row-altered` |
| `forge-a-prune-anchor` | plant a pruned-anchor hash so the chain appears to start after a prune that never happened | 8 | 8/8 | `chain-break` |
| `forge-an-empty-session-scope` | invent a session ledger that never existed, committed to zero receipts | 8 | 8/8 | `phantom-scope` |
| `lie-about-uncommitted-rows` | inflate the pre-chain row counter to reserve room for a forgery | 8 | 8/8 | `uncommitted-row` |
| `malformed-balanced-out-of-range` | store a verdict that is neither 0 nor 1 and let every reader coerce it its own way | 8 | 8/8 | `malformed-row` |
| `malformed-delta-nonnumeric` | put text in the money column: a coerced NaN must not read as the committed null | 8 | 8/8 | `malformed-row` |
| `malformed-findingtypes-padding` | pad the finding list with empty members — a different stored row that decodes the same | 8 | 8/8 | `malformed-row` |
| `malformed-seq-null` | drop a chained row's position so it sorts wherever the engine feels like | 8 | 8/8 | `malformed-row` |
| `malformed-seq-past-readable-range` | push a position past 2^53, where READING it is what fails — a verifier that dies is not a verifier that reports | 8 | 8/8 | `malformed-row` |
| `null-a-chain-column` | blank one chain column so the row falls out of the walk | 8 | 8/8 | `chain-break`, `uncommitted-row` |
| `reinsert-deleted-row` | delete a receipt and paste it back at the end, chain columns and all | 8 | 8/8 | `chain-break` |
| `reorder-rows` | swap two receipts' positions so the story reads differently | 8 | 8/8 | `chain-break` |
| `rewrite-scope-count` | adjust a scope's committed receipt count to cover a removal | 8 | 8/8 | `count-mismatch`, `malformed-state` |
| `rewrite-scope-head` | point a scope's head somewhere else | 8 | 8/8 | `head-mismatch` |
| `rewrite-scope-root` | swap in a Merkle root that commits to a different set | 8 | 8/8 | `root-mismatch` |
| `rewrite-scope-rootcount-negative` | make the Merkle check vacuous with a NEGATIVE rootCount and the empty root | 8 | 8/8 | `malformed-state` |
| `rewrite-scope-rootcount-nonnumeric` | same evasion with a NON-NUMERIC rootCount — a comparison against NaN is always false | 8 | 8/8 | `malformed-state` |
| `splice-row-into-another-scope` | move a receipt into someone else's session ledger | 8 | 8/8 | `row-altered` |
| `truncate-tail` | lop the newest receipts off the end — a valid chain, just a shorter one | 8 | 8/8 | `count-mismatch` |
| `wipe-every-chain-column` | strip the whole chain and leave the receipts, so the store looks like it predates it | 8 | 8/8 | `count-mismatch`, `uncommitted-row` |
| `zero-out-a-scope` | reset one scope's commitment to 'empty ledger' while its receipts are still there | 8 | 8/8 | `count-mismatch` |

## Escapes

None. Every generated tamper in the in-model space was caught and located.

## Pinned boundaries — these must still VERIFY

The chain proves internal consistency, not authorship. These are the limits stated in
[docs/VERIFICATION.md](VERIFICATION.md); the drill pins them so that if one ever moves, it moves
deliberately and the claim gets re-stated with it.

| class | why it is out of the model | cases | still verifies |
|---|---|---:|---:|
| `boundary-full-reseal-rewrite` | edit a receipt and RE-DERIVE every leaf, link and commitment — self-consistent, and only an off-box anchor can tell | 8 | 8/8 |
| `boundary-full-reseal-wipe` | delete every receipt AND reset every commitment: an empty ledger is a valid ledger | 8 | 8/8 |
| `boundary-prefix-prune` | delete the OLDEST receipts and record it as a prune — indistinguishable from bounded retention, which is the point of retention | 8 | 8/8 |
| `boundary-seen-count` | bump the replay counter: seenCount is mutable by design and deliberately outside the leaf | 8 | 8/8 |
| `boundary-seq-renumber` | renumber every position while KEEPING their order: seq is read as an order and is not among the facts a leaf commits to | 8 | 8/8 |

Catching a full re-seal from the file alone is impossible by construction — the adversary rewrites the evidence and the record of the evidence in one pass. It needs an off-box anchor (a published root, or a signature over it), which is why the roots are exposed rather than kept internal.

**The anchor exists now, so the two re-seal classes stay boundaries only for the file-alone walk.** `npm run anchor:ledger -- <db> --out anchor.json` commits to the whole store's head, root and count; `npm run verify:ledger -- <db> --anchor anchor.json` catches both with a located `anchor-mismatch`. They remain listed here as boundaries because that is what they are *for the unanchored walk this drill runs*, and moving them into the in-model set would claim a detection the drill does not itself perform. The anchored path is proven separately in [`ledger-anchor.test.ts`](../lib/store/ledger-anchor.test.ts).
