/**
 * Pacioli ledger contract.
 *
 * The canonical types now live with the engine at `lib/engine/types.ts`, so the
 * data and the engine that scores it can never drift. This file is kept as the
 * dataset's stable, self-describing import path (and is referenced by TAXONOMY.md).
 */
export * from "../lib/engine/types";
